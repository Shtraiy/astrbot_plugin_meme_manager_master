# 修复设计

## 目标

修复已有表情包索引批量识别结果被错误写成“未知”的问题，并减少后台索引对 meme_manager 数据文件的无意义重复写入，避免放大 WebUI 的重复请求和日志噪声。

## 方案

1. 批量模型结果按“规范 ID → 文件名/文件 stem → 完整结果顺序”逐级关联；只有结果数量完整时才启用顺序兜底，避免把部分结果错配到其他图片。
2. 后台索引只在目录图片签名或索引元数据发生变化时执行；索引内容未变化时不重写 `index.json` 和 `README.md`。
3. 批量识别失败或返回不完整结果时保留待重试状态，并对相同源文件签名进行退避，下一次健康检查再重试，不在每次健康检查中重复调用模型。
4. 不修改依赖插件的 WebUI 路由；对本仓库能控制的文件写入和索引调度做降噪保护。

## 验证

覆盖批量结果按文件名、按顺序、部分缺失结果的关联行为，以及无变化索引不重复写入和失败退避。
