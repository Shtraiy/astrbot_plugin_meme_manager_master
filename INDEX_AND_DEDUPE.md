# 分类索引与感知去重

## 平铺主索引与标签快速索引

当前表情包图片统一保存在 `memes/` 根目录，`memes/index.json` 是唯一主索引：

- 同一张图片只保存一个 `meme_<sha256前12位>.<ext>` 文件。
- 一个条目可以拥有最多 6 个固定标签；不同标签不会创建第二份图片。
- `index_version`、`index_prompt_version`、`index_provider_id` 继续用于判断识别结果是否需要更新。

`memes/tag_index.json` 是由主索引派生的按标签候选索引。Bot 选图正常情况下只读取该文件和主索引文件时间戳；索引缺失、损坏或失效时，会自动从 `index.json` 重建。管理页面的分类仍是虚拟标签桶，同一文件可以同时出现在多个标签下。

只有图片摘要的 `sha256`、识别版本签名都匹配当前配置，且条目为 `indexed: true` 时，后台扫描才会复用旧结果。旧索引或切换识别配置后，插件会重新分类，但不会为同一图片创建多个标签文件。

## 感知去重

保存前先做 SHA-256 精确去重，再使用 Pillow 计算带亮度信息的 8x7 平均感知哈希，识别常见的缩放、重新压缩等视觉等价图片。命中重复图片时会合并本次识别出的标签；默认汉明距离阈值为 6，阈值越小越严格。相同消息中的多张相似图片仍会合并过滤。

重复图片不会进入表情索引页的待分类队列。命中已有图片时，输入图片的 SHA-256 会写入插件级 `capture_auto_blacklist.json`，同时记录来源资源包和文件名；已有重复活动记录会迁移为已处理状态。自动黑名单只在来源图片仍存在时生效，来源图片删除后会自动清理。用户主动忽略或删除图片产生的 `capture_blacklist.json` 属于永久手动黑名单，不会因资源包图片删除而移除。

配置项：

- `perceptual_dedupe_enabled`：关闭后仍保留 SHA-256 精确去重。
- `perceptual_duplicate_threshold`：默认 `6`，建议 Gemini 用户先保持默认值；误判相似图时可调低到 `3` 或 `4`。

Pillow 无法加载图片或尚未安装时，会安全回退到 SHA-256，不会阻塞插件启动。
